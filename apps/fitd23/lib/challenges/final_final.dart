import 'package:flutter/material.dart';

class FinalFinal extends StatelessWidget {
  static const String path = "/lukas-vs-alek";

  const FinalFinal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Material(
      child: Center(child: Text("Draw your workplace")),
    );
  }
}
